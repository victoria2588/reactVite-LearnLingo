export const selectTeachers = (state) => state.teachers.teachersCard;
export const selectIsLoading = (state) => state.teachers.isLoading;
export const selectFavorite = (state) => state.teachers.favorite;
export const selectCard = (state) => state.teachers.card;
